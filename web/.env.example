# Google Cloud API configuration
# SECURITY: Replace with real values for your environment
GOOGLE_API_KEY=your-api-key-here-replace-with-real-key
GOOGLE_GENAI_MODEL=gemini-2.5-flash
GOOGLE_CLOUD_PROJECT=your-project-id-here

# Redis Configuration (optional, for distributed caching)
REDIS_URL=redis://localhost:6379/0
CACHE_TTL=3600

# System Configuration
MADSPARK_MODE=mock
MAX_CONCURRENT_AGENTS=10
ENVIRONMENT=development

# NOTE: LLM Provider abstraction (MADSPARK_LLM_PROVIDER, MADSPARK_MODEL_TIER, etc.)
# is Phase 1 infrastructure for CLI only. Web backend still uses Gemini directly
# and does not yet support provider switching or the LLM router.